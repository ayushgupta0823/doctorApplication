name: mediconnect_doctor_app
description: MediConnectAI - Doctor App (Flutter port of the doctor_app.html prototype)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.6
  permission_handler: ^11.3.1
  local_auth: ^2.3.0
  connectivity_plus: ^6.1.0
  shared_preferences: ^2.3.3
  pdf: ^3.11.1
  printing: ^5.13.3
  dio: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
